# Backend origin the admin panel proxies /api/v1 to.
# Local/default for the Bondi VPS (host nginx via docker bridge):
#   http://172.17.0.1:80
# For a separate host: e.g. https://api.yourdomain.com
BACKEND_ORIGIN=http://172.17.0.1:80