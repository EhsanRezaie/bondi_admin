import { PlaceholderPage } from '../components/PlaceholderPage';

export function TicketsPage() {
  return <PlaceholderPage title="Tickets" description="Support tickets is coming in a later milestone." />;
}