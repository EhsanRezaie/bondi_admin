import { PlaceholderPage } from '../components/PlaceholderPage';

export function UsersPage() {
  return <PlaceholderPage title="Users" description="User management is coming in the next milestone." />;
}